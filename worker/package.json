{
  "name": "cosense-uploader-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/strip.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
